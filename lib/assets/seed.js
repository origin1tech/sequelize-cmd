'use strict';

/**
 * Seeds data to database upon migration.
 * @param {sequelize} db - the injected Sequelize instance.
 * @param {object} types - the injected Sequelize DataTypes.
 * @param {Chance} chance - chance utility for generating seed data.
 */
module.exports = function (db, types, chance) {

   // see "seed.example.js" in seeds directory for basic usage.

};
